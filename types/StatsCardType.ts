export type StatType = {
    id: string
    label: string,
    value: string,
    delta: string,
    deltaPositive: boolean,
    icon: string,
    gradient: [string, string],
    accentColor: string,
}
